<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Personal AI</title>
    <link rel="icon" type="image/png" href="/icons/logo.png" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
      integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/home.css" />
  </head>
  <body>
    <div class="header">
      <div class="logo">
        <a href="/home">
          <img src="/icons/logo.png" class="icons" alt="logo" />
        </a>
      </div>
      <div class="profile">
        <% if (user && user.photos && user.photos.length > 0) { %>
        <img
          src="<%= user.photos[0].value %>"
          alt="Profile picture"
          title="<%= user.displayName %>"
        />
        <% } else { %>
        <img
          src="/icons/profile.png"
          alt="Default profile picture"
          title="<%= user ? user.displayName : 'User' %>"
        />
        <% } %>
      </div>
    </div>

    <div class="settings">
      <button class="info"><%=user.displayName%> <%=user.email%></button>

      <button class="options">
        Language &nbsp;<i class="fa-solid fa-language" id="icons"></i>
      </button>
      <button class="options">Theme</button>
      <button class="options">
        Give feedback<i class="ri-feedback-line" id="icons"></i>
      </button>
      <button class="options">About</button>
      <button class="logout">
        Sign out &nbsp;&nbsp;<i class="ri-logout-box-r-line"></i>
      </button>
      <div class="links">
        <a type="">Privacy</a> &nbsp;&nbsp; <a src="">Terms</a>&nbsp;&nbsp;
        <a src="">FAQ</a>&nbsp;&nbsp;
      </div>
    </div>

    <div class="centre" id="chat-box">
      <div
        id="typing-text"
        data-text="Hello <%= user.displayName %>, Welcome to your Personal Assistant!"
      ></div>
      <!-- <div class="preview"></div> -->
    </div>
    <div id="mainPreviewContainer">
    <div class="preview-container" id="preview-container" style="display: none;">
      <img id="file-preview" alt="File Preview" style="display: none;">
      <img id="file-icon" class="file-icon" alt="File Icon" style="display: none;">
      <div class="file-info" id="file-name">No file chosen</div>
      <button class="remove-btn" id="remove-btn" onclick="removeFile()">✖</button>
  </div>
</div>
  <div class="footer">
      <form class="search" enctype="multipart/form-data">
        <input type="file" id="file-input" name="file-input" hidden multiple onchange="displayFile()"/>
        <label for="file-input" class="upload-label">
          <i class="ri-upload-2-line"></i>
        </label>
        <input type="text" id="text-input" placeholder="What do you want!" />
        <button class="send">
          <i class="ri-arrow-up-line"></i>
        </button>
      </form>
    </div>

    <script src="../js/home.js"></script>
  </body>
</html>
